#pragma once

#include <glm/glm.hpp>

class Player
{
public:
	enum PlayerPosition
	{
		LEFT = 1,
		MIDDLE = 0,
		RIGHT = -1
	};
private:
public:
};

enum PlayerPosition
{
	LEFT = 1,
	MIDDLE = 0,
	RIGHT = -1
};